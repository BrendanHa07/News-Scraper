<br>
<div class="container">
  <div class="row">
      <h3>{{result.title}}</h3>
      <h5>{{result.summary}}</h5>
  </div>

  <div class="row">
    <h3>Comments So Far</h3>
  </div>
  <div class="row">
      {{#unless result.Comments}}
        <p>There are no comments. Add yours below!</p>
      {{/unless}}
  </div>
  <div class="row">
       <ul>
        {{#each result.Comments}}
        <li>
          <h5>Name: {{result.name}}</h5>
          <p>Message: {{result.body}}></p>
        </li>
        {{/each}}
        <button class="delete-button btn btn-primary" data-comment="{{this._id}}">Delete Comment</button>
      </ul>
  </div>
   
  

  <div class="row">
    <h2>Add a comment!</h2>
  </div>
  <form action="/articles/{{result._id}}">
    <div class="form-group row">
        <label for="name" class="col-form-label">Name</label>
        <input type="text" class="form-control" name="name" id="name" required>
    </div>
    <div class="form-group row">
        <label for="body">Comment</label>
        <textarea class="form-control" name="body" id="body"  rows="3"></textarea>
    </div>
    <button class="btn btn-primary">Submit</button>
  </form>

  <br>
  <br>
  <br>
  <footer class="footer">
        <p>&copy; Brendan Ha 2017</p>
  </footer>
</div>
